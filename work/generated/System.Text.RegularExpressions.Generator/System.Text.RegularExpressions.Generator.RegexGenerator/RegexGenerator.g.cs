// <auto-generated/>
#nullable enable
#pragma warning disable

namespace Purview.EventSourcing.SqlServer.Client
{
    partial class SqlServerClient
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[\\w\\-\\.]+$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match a character in the set [\-.\w] atomically at least once.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
        private static partial global::System.Text.RegularExpressions.Regex IdentifierRegex() => global::System.Text.RegularExpressions.Generated.IdentifierRegex_0.Instance;
    }
}

namespace Purview.EventSourcing.SqlServer.Events
{
    partial class SqlServerEventStoreClient
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[\\w\\-\\.]+$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match a character in the set [\-.\w] atomically at least once.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
        private static partial global::System.Text.RegularExpressions.Regex IdentifierRegex() => global::System.Text.RegularExpressions.Generated.IdentifierRegex_0.Instance;
    }
}

namespace Purview.EventSourcing.SqlServer
{
    partial class SqlServerJsonIndexSchemaManager
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[\\w\\-\\.]+$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match a character in the set [\-.\w] atomically at least once.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
        private static partial global::System.Text.RegularExpressions.Regex IdentifierRegex() => global::System.Text.RegularExpressions.Generated.IdentifierRegex_0.Instance;
    }
}

namespace Purview.EventSourcing.SqlServer
{
    partial class SqlServerJsonIndexSchemaManager
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[A-Za-z][A-Za-z0-9]*(?:\\s*\\(\\s*\\d+\\s*(?:,\\s*\\d+\\s*)?\\))?$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match an ASCII letter.<br/>
        /// ○ Match an ASCII letter or digit greedily any number of times.<br/>
        /// ○ Optional (greedy).<br/>
        ///     ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ Match '('.<br/>
        ///     ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ Match a Unicode digit greedily at least once.<br/>
        ///     ○ Match a whitespace character greedily any number of times.<br/>
        ///     ○ Optional (greedy).<br/>
        ///         ○ Match ','.<br/>
        ///         ○ Match a whitespace character atomically any number of times.<br/>
        ///         ○ Match a Unicode digit greedily at least once.<br/>
        ///         ○ Match a whitespace character atomically any number of times.<br/>
        ///     ○ Match ')'.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
        private static partial global::System.Text.RegularExpressions.Regex SqlTypeRegex() => global::System.Text.RegularExpressions.Generated.SqlTypeRegex_1.Instance;
    }
}

namespace Purview.EventSourcing.SqlServer
{
    partial class SqlServerJsonIndexSchemaManager
    {
        /// <remarks>
        /// Pattern:<br/>
        /// <code>^[\\[\\]\\w\\s\\=\\&lt;\\&gt;\\!\\(\\)'\\.,-]+$</code><br/>
        /// Explanation:<br/>
        /// <code>
        /// ○ Match if at the beginning of the string.<br/>
        /// ○ Match a character in the set [!'-),-.&lt;-&gt;[]\w\s] greedily at least once.<br/>
        /// ○ Match if at the end of the string or if before an ending newline.<br/>
        /// </code>
        /// </remarks>
        [global::System.CodeDom.Compiler.GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
        private static partial global::System.Text.RegularExpressions.Regex FilterRegex() => global::System.Text.RegularExpressions.Generated.FilterRegex_2.Instance;
    }
}

namespace System.Text.RegularExpressions.Generated
{
    using System;
    using System.Buffers;
    using System.CodeDom.Compiler;
    using System.Collections;
    using System.ComponentModel;
    using System.Globalization;
    using System.Runtime.CompilerServices;
    using System.Text.RegularExpressions;
    using System.Threading;

    /// <summary>Custom <see cref="Regex"/>-derived type for the IdentifierRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
    file sealed class IdentifierRegex_0 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly IdentifierRegex_0 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private IdentifierRegex_0()
        {
            base.pattern = "^[\\w\\-\\.]+$";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match a character in the set [\-.\w] atomically at least once.
                    {
                        int iteration = 0;
                        while ((uint)iteration < (uint)slice.Length && ((ch = slice[iteration]) < 128 ? ("\0\0怀Ͽ\ufffe蟿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\u0002\n-/\0\u0002\u0004\u0005\u0003\u0001\u0006\t\u0013\0")))
                        {
                            iteration++;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                    }
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        return false; // The input didn't match.
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the SqlTypeRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
    file sealed class SqlTypeRegex_1 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly SqlTypeRegex_1 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private SqlTypeRegex_1()
        {
            base.pattern = "^[A-Za-z][A-Za-z0-9]*(?:\\s*\\(\\s*\\d+\\s*(?:,\\s*\\d+\\s*)?\\))?$";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    int charloop_starting_pos1 = 0, charloop_ending_pos1 = 0;
                    int charloop_starting_pos2 = 0, charloop_ending_pos2 = 0;
                    int charloop_starting_pos3 = 0, charloop_ending_pos3 = 0;
                    int loop_iteration = 0;
                    int loop_iteration1 = 0;
                    int stackpos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match an ASCII letter.
                    if (slice.IsEmpty || !char.IsAsciiLetter(slice[0]))
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match an ASCII letter or digit greedily any number of times.
                    //{
                        pos++;
                        slice = inputSpan.Slice(pos);
                        charloop_starting_pos = pos;
                        
                        int iteration = slice.IndexOfAnyExcept(Utilities.s_asciiLettersAndDigits);
                        if (iteration < 0)
                        {
                            iteration = slice.Length;
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                        
                        charloop_ending_pos = pos;
                        goto CharLoopEnd;
                        
                        CharLoopBacktrack:
                        
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (charloop_starting_pos >= charloop_ending_pos)
                        {
                            return false; // The input didn't match.
                        }
                        pos = --charloop_ending_pos;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd:
                    //}
                    
                    // Optional (greedy).
                    //{
                        loop_iteration = 0;
                        
                        LoopBody:
                        Utilities.StackPush(ref base.runstack!, ref stackpos, pos);
                        
                        loop_iteration++;
                        
                        // Match a whitespace character atomically any number of times.
                        {
                            int iteration1 = 0;
                            while ((uint)iteration1 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration1]))
                            {
                                iteration1++;
                            }
                            
                            slice = slice.Slice(iteration1);
                            pos += iteration1;
                        }
                        
                        // Match '('.
                        if (slice.IsEmpty || slice[0] != '(')
                        {
                            goto LoopIterationNoMatch;
                        }
                        
                        // Match a whitespace character atomically any number of times.
                        {
                            int iteration2 = 1;
                            while ((uint)iteration2 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration2]))
                            {
                                iteration2++;
                            }
                            
                            slice = slice.Slice(iteration2);
                            pos += iteration2;
                        }
                        
                        // Match a Unicode digit greedily at least once.
                        //{
                            charloop_starting_pos1 = pos;
                            
                            int iteration3 = 0;
                            while ((uint)iteration3 < (uint)slice.Length && char.IsDigit(slice[iteration3]))
                            {
                                iteration3++;
                            }
                            
                            if (iteration3 == 0)
                            {
                                goto LoopIterationNoMatch;
                            }
                            
                            slice = slice.Slice(iteration3);
                            pos += iteration3;
                            
                            charloop_ending_pos1 = pos;
                            charloop_starting_pos1++;
                            goto CharLoopEnd1;
                            
                            CharLoopBacktrack1:
                            Utilities.StackPop(base.runstack!, ref stackpos, out charloop_ending_pos1, out charloop_starting_pos1);
                            
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (charloop_starting_pos1 >= charloop_ending_pos1)
                            {
                                goto LoopIterationNoMatch;
                            }
                            pos = --charloop_ending_pos1;
                            slice = inputSpan.Slice(pos);
                            
                            CharLoopEnd1:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, charloop_starting_pos1, charloop_ending_pos1);
                        //}
                        
                        // Match a whitespace character greedily any number of times.
                        //{
                            charloop_starting_pos2 = pos;
                            
                            int iteration4 = 0;
                            while ((uint)iteration4 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration4]))
                            {
                                iteration4++;
                            }
                            
                            slice = slice.Slice(iteration4);
                            pos += iteration4;
                            
                            charloop_ending_pos2 = pos;
                            goto CharLoopEnd2;
                            
                            CharLoopBacktrack2:
                            Utilities.StackPop(base.runstack!, ref stackpos, out charloop_ending_pos2, out charloop_starting_pos2);
                            
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (charloop_starting_pos2 >= charloop_ending_pos2)
                            {
                                goto CharLoopBacktrack1;
                            }
                            pos = --charloop_ending_pos2;
                            slice = inputSpan.Slice(pos);
                            
                            CharLoopEnd2:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, charloop_starting_pos2, charloop_ending_pos2);
                        //}
                        
                        // Optional (greedy).
                        //{
                            loop_iteration1 = 0;
                            
                            LoopBody1:
                            Utilities.StackPush(ref base.runstack!, ref stackpos, pos);
                            
                            loop_iteration1++;
                            
                            // Match ','.
                            if (slice.IsEmpty || slice[0] != ',')
                            {
                                goto LoopIterationNoMatch1;
                            }
                            
                            // Match a whitespace character atomically any number of times.
                            {
                                int iteration5 = 1;
                                while ((uint)iteration5 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration5]))
                                {
                                    iteration5++;
                                }
                                
                                slice = slice.Slice(iteration5);
                                pos += iteration5;
                            }
                            
                            // Match a Unicode digit greedily at least once.
                            //{
                                charloop_starting_pos3 = pos;
                                
                                int iteration6 = 0;
                                while ((uint)iteration6 < (uint)slice.Length && char.IsDigit(slice[iteration6]))
                                {
                                    iteration6++;
                                }
                                
                                if (iteration6 == 0)
                                {
                                    goto LoopIterationNoMatch1;
                                }
                                
                                slice = slice.Slice(iteration6);
                                pos += iteration6;
                                
                                charloop_ending_pos3 = pos;
                                charloop_starting_pos3++;
                                goto CharLoopEnd3;
                                
                                CharLoopBacktrack3:
                                Utilities.StackPop(base.runstack!, ref stackpos, out charloop_ending_pos3, out charloop_starting_pos3);
                                
                                if (Utilities.s_hasTimeout)
                                {
                                    base.CheckTimeout();
                                }
                                
                                if (charloop_starting_pos3 >= charloop_ending_pos3)
                                {
                                    goto LoopIterationNoMatch1;
                                }
                                pos = --charloop_ending_pos3;
                                slice = inputSpan.Slice(pos);
                                
                                CharLoopEnd3:
                                Utilities.StackPush(ref base.runstack!, ref stackpos, charloop_starting_pos3, charloop_ending_pos3);
                            //}
                            
                            // Match a whitespace character atomically any number of times.
                            {
                                int iteration7 = 0;
                                while ((uint)iteration7 < (uint)slice.Length && char.IsWhiteSpace(slice[iteration7]))
                                {
                                    iteration7++;
                                }
                                
                                slice = slice.Slice(iteration7);
                                pos += iteration7;
                            }
                            
                            
                            // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                            if (loop_iteration1 == 0)
                            {
                                goto LoopBody1;
                            }
                            goto LoopEnd1;
                            
                            // The loop iteration failed. Put state back to the way it was before the iteration.
                            LoopIterationNoMatch1:
                            if (--loop_iteration1 < 0)
                            {
                                // Unable to match the remainder of the expression after exhausting the loop.
                                goto CharLoopBacktrack2;
                            }
                            pos = base.runstack![--stackpos];
                            slice = inputSpan.Slice(pos);
                            goto LoopEnd1;
                            
                            LoopBacktrack:
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            if (loop_iteration1 == 0)
                            {
                                // No iterations of the loop remain to backtrack into. Fail the loop.
                                goto CharLoopBacktrack2;
                            }
                            goto CharLoopBacktrack3;
                            LoopEnd1:
                            
                            Utilities.StackPush(ref base.runstack!, ref stackpos, loop_iteration1);
                            goto LoopSkipBacktrack;
                            
                            LoopBacktrack1:
                            loop_iteration1 = base.runstack![--stackpos];
                            if (Utilities.s_hasTimeout)
                            {
                                base.CheckTimeout();
                            }
                            
                            goto LoopBacktrack;
                            
                            LoopSkipBacktrack:;
                        //}
                        
                        // Match ')'.
                        if (slice.IsEmpty || slice[0] != ')')
                        {
                            goto LoopBacktrack1;
                        }
                        
                        pos++;
                        slice = inputSpan.Slice(pos);
                        
                        // The loop has an upper bound of 1. Continue iterating greedily if it hasn't yet been reached.
                        if (loop_iteration == 0)
                        {
                            goto LoopBody;
                        }
                        goto LoopEnd;
                        
                        // The loop iteration failed. Put state back to the way it was before the iteration.
                        LoopIterationNoMatch:
                        if (--loop_iteration < 0)
                        {
                            // Unable to match the remainder of the expression after exhausting the loop.
                            goto CharLoopBacktrack;
                        }
                        pos = base.runstack![--stackpos];
                        slice = inputSpan.Slice(pos);
                        goto LoopEnd;
                        
                        LoopBacktrack2:
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (loop_iteration == 0)
                        {
                            // No iterations of the loop remain to backtrack into. Fail the loop.
                            goto CharLoopBacktrack;
                        }
                        goto LoopBacktrack1;
                        LoopEnd:;
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto LoopBacktrack2;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Custom <see cref="Regex"/>-derived type for the FilterRegex method.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
    file sealed class FilterRegex_2 : Regex
    {
        /// <summary>Cached, thread-safe singleton instance.</summary>
        internal static readonly FilterRegex_2 Instance = new();
    
        /// <summary>Initializes the instance.</summary>
        private FilterRegex_2()
        {
            base.pattern = "^[\\[\\]\\w\\s\\=\\<\\>\\!\\(\\)'\\.,-]+$";
            base.roptions = RegexOptions.None;
            ValidateMatchTimeout(Utilities.s_defaultTimeout);
            base.internalMatchTimeout = Utilities.s_defaultTimeout;
            base.factory = new RunnerFactory();
            base.capsize = 1;
        }
            
        /// <summary>Provides a factory for creating <see cref="RegexRunner"/> instances to be used by methods on <see cref="Regex"/>.</summary>
        private sealed class RunnerFactory : RegexRunnerFactory
        {
            /// <summary>Creates an instance of a <see cref="RegexRunner"/> used by methods on <see cref="Regex"/>.</summary>
            protected override RegexRunner CreateInstance() => new Runner();
        
            /// <summary>Provides the runner that contains the custom logic implementing the specified regular expression.</summary>
            private sealed class Runner : RegexRunner
            {
                /// <summary>Scan the <paramref name="inputSpan"/> starting from base.runtextstart for the next match.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                protected override void Scan(ReadOnlySpan<char> inputSpan)
                {
                    // The pattern is anchored.  Validate the current position and try to match at it only.
                    if (TryFindNextPossibleStartingPosition(inputSpan) && !TryMatchAtCurrentPosition(inputSpan))
                    {
                        base.runtextpos = inputSpan.Length;
                    }
                }
        
                /// <summary>Search <paramref name="inputSpan"/> starting from base.runtextpos for the next location a match could possibly start.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if a possible match was found; false if no more matches are possible.</returns>
                private bool TryFindNextPossibleStartingPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    
                    // Empty matches aren't possible.
                    if ((uint)pos < (uint)inputSpan.Length)
                    {
                        // The pattern leads with a beginning (\A) anchor.
                        if (pos == 0)
                        {
                            return true;
                        }
                    }
                    
                    // No match found.
                    base.runtextpos = inputSpan.Length;
                    return false;
                }
        
                /// <summary>Determine whether <paramref name="inputSpan"/> at base.runtextpos is a match for the regular expression.</summary>
                /// <param name="inputSpan">The text being scanned by the regular expression.</param>
                /// <returns>true if the regular expression matches at the current position; otherwise, false.</returns>
                private bool TryMatchAtCurrentPosition(ReadOnlySpan<char> inputSpan)
                {
                    int pos = base.runtextpos;
                    int matchStart = pos;
                    char ch;
                    int charloop_starting_pos = 0, charloop_ending_pos = 0;
                    ReadOnlySpan<char> slice = inputSpan.Slice(pos);
                    
                    // Match if at the beginning of the string.
                    if (pos != 0)
                    {
                        return false; // The input didn't match.
                    }
                    
                    // Match a character in the set [!'-),-.<->[]\w\s] greedily at least once.
                    //{
                        charloop_starting_pos = pos;
                        
                        int iteration = 0;
                        while ((uint)iteration < (uint)slice.Length && ((ch = slice[iteration]) < 128 ? ("㸀\0玃珿\ufffe꿿\ufffe߿"[ch >> 4] & (1 << (ch & 0xF))) != 0 : RegexRunner.CharInClass((char)ch, "\0\f\v!\"'*,/<?[\\]^\0\u0002\u0004\u0005\u0003\u0001\u0006\t\u0013\0d")))
                        {
                            iteration++;
                        }
                        
                        if (iteration == 0)
                        {
                            return false; // The input didn't match.
                        }
                        
                        slice = slice.Slice(iteration);
                        pos += iteration;
                        
                        charloop_ending_pos = pos;
                        charloop_starting_pos++;
                        goto CharLoopEnd;
                        
                        CharLoopBacktrack:
                        
                        if (Utilities.s_hasTimeout)
                        {
                            base.CheckTimeout();
                        }
                        
                        if (charloop_starting_pos >= charloop_ending_pos)
                        {
                            return false; // The input didn't match.
                        }
                        pos = --charloop_ending_pos;
                        slice = inputSpan.Slice(pos);
                        
                        CharLoopEnd:
                    //}
                    
                    // Match if at the end of the string or if before an ending newline.
                    if (pos < inputSpan.Length - 1 || ((uint)pos < (uint)inputSpan.Length && inputSpan[pos] != '\n'))
                    {
                        goto CharLoopBacktrack;
                    }
                    
                    // The input matched.
                    base.runtextpos = pos;
                    base.Capture(0, matchStart, pos);
                    return true;
                }
            }
        }

    }
    
    /// <summary>Helper methods used by generated <see cref="Regex"/>-derived implementations.</summary>
    [GeneratedCodeAttribute("System.Text.RegularExpressions.Generator", "10.0.14.37416")]
    file static class Utilities
    {
        /// <summary>Default timeout value set in <see cref="AppContext"/>, or <see cref="Regex.InfiniteMatchTimeout"/> if none was set.</summary>
        internal static readonly TimeSpan s_defaultTimeout = AppContext.GetData("REGEX_DEFAULT_MATCH_TIMEOUT") is TimeSpan timeout ? timeout : Regex.InfiniteMatchTimeout;
        
        /// <summary>Whether <see cref="s_defaultTimeout"/> is non-infinite.</summary>
        internal static readonly bool s_hasTimeout = s_defaultTimeout != Regex.InfiniteMatchTimeout;
        
        /// <summary>Pops 2 values from the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPop(int[] stack, ref int pos, out int arg0, out int arg1)
        {
            arg0 = stack[--pos];
            arg1 = stack[--pos];
        }
        
        /// <summary>Pushes 1 value onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0)
        {
            // If there's space available for the value, store it.
            int[] s = stack;
            int p = pos;
            if ((uint)p < (uint)s.Length)
            {
                s[p] = arg0;
                pos++;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0);
        
            // <summary>Resize the backtracking stack array and push 1 value onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0)
            {
                Array.Resize(ref stack, (pos + 0) * 2);
                StackPush(ref stack, ref pos, arg0);
            }
        }
        
        /// <summary>Pushes 2 values onto the backtracking stack.</summary>
        [MethodImpl(MethodImplOptions.AggressiveInlining)]
        internal static void StackPush(ref int[] stack, ref int pos, int arg0, int arg1)
        {
            // If there's space available for all 2 values, store them.
            int[] s = stack;
            int p = pos;
            if ((uint)(p + 1) < (uint)s.Length)
            {
                s[p] = arg0;
                s[p + 1] = arg1;
                pos += 2;
                return;
            }
        
            // Otherwise, resize the stack to make room and try again.
            WithResize(ref stack, ref pos, arg0, arg1);
        
            // <summary>Resize the backtracking stack array and push 2 values onto the stack.</summary>
            [MethodImpl(MethodImplOptions.NoInlining)]
            static void WithResize(ref int[] stack, ref int pos, int arg0, int arg1)
            {
                Array.Resize(ref stack, (pos + 1) * 2);
                StackPush(ref stack, ref pos, arg0, arg1);
            }
        }
        
        /// <summary>Supports searching for characters in or not in "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz".</summary>
        internal static readonly SearchValues<char> s_asciiLettersAndDigits = SearchValues.Create("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz");
    }
}
