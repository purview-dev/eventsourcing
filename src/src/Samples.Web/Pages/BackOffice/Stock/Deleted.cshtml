@page
@model Purview.EventSourcing.Samples.Web.Pages.BackOffice.Stock.DeletedModel
@{
    ViewData["Title"] = "Archived Stock Items";
}

<div class="mb-6">
    <a asp-page="Index" class="text-sm text-slate-500 hover:underline">← Back to Stock</a>
</div>

@if (Model.DeletedItems.Count == 0)
{
    <div class="text-center py-16 text-gray-400">
        <p class="text-5xl mb-3">🗄</p>
        <p class="text-lg font-medium">No archived stock items</p>
    </div>
}
else
{
    <div class="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden">
        <table class="w-full text-sm">
            <thead class="bg-gray-50 border-b border-gray-100">
                <tr>
                    <th class="text-left px-5 py-3 font-medium text-gray-600">Product Name</th>
                    <th class="text-left px-5 py-3 font-medium text-gray-600">Product ID</th>
                    <th class="text-left px-5 py-3 font-medium text-gray-600">Location</th>
                    <th class="px-5 py-3"></th>
                </tr>
            </thead>
            <tbody class="divide-y divide-gray-50">
                @foreach (var item in Model.DeletedItems)
                {
                    <tr class="hover:bg-gray-50">
                        <td class="px-5 py-3 font-medium text-gray-500">@item.ProductName</td>
                        <td class="px-5 py-3 font-mono text-xs text-gray-400">@item.ProductId</td>
                        <td class="px-5 py-3 text-gray-400">@item.LocationName</td>
                        <td class="px-5 py-3 text-right">
                            <form method="post" asp-page-handler="Restore" asp-route-id="@item.Id()" class="inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="text-green-600 hover:underline text-sm">Restore</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
